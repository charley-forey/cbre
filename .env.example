# Optional: shared n8n demo password for flows.creai.solutions (shown on the landing page).
# Set the same variable in Vercel → Settings → Environment Variables for production.
# Never commit real secrets to git; use hosting env or local .env*.local only.
NEXT_PUBLIC_N8N_DEMO_PASSWORD=
